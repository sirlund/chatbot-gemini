{
  "name": "chatbot-gemini",
  "version": "1.0.0",
  "description": "Chatbot simple usando la API de Gemini",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
