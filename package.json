{
  "name": "chatbot-gemini",
  "version": "1.0.0",
  "description": "Chatbot con Gemini API y RAG para Design System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@llamaindex/google": "^0.4.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "llamaindex": "^0.12.1"
  },
  "engines": {
    "node": ">=18"
  }
}
